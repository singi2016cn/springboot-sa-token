<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.singi.demo.mapper.BookMapper">
    <select id="selectBookById">select * from book where id = #{id}</select>

    <select id="selectBookAndProvince">select * from book,province limit 5</select>
</mapper>